#!/usr/bin/env bash
# 02-test-providers-crud.sh
# Template for provider CRUD tests. Implement similar to events CRUD in 01-test-events-crud.sh

set -euo pipefail

echo "This is a template. Copy 01-test-events-crud.sh and adapt endpoints for providers CRUD.
Example provider endpoints:
  POST /api/providers/
  GET /api/providers/
  GET /api/providers/{id}
  PUT /api/providers/{id}
  DELETE /api/providers/{id}
"

exit 0
